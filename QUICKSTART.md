# 빠른 시작 가이드

프로젝트를 **5분 안에** 실행하는 방법입니다.

## 1단계: 데이터 업로드 (최우선!)

챗봇을 실행하기 전에 반드시 Q&A 데이터를 Pinecone에 업로드해야 합니다.

### 업로드 실행
```bash
npm run upload
```

### 정상 출력 예시
```
🚀 Starting data upload process...

📂 Reading file: C:\Users\...\data\[ESTSoft]+바이브+코딩+인턴+샘플+Q_A+데이터+1.xlsx
✅ Parsed 13 Q&A pairs

📋 First 2 Q&A pairs:

[1]
Q: 바이브코딩 인턴 지원자격이 어떻게 되나요?
A: 바이브코딩 인턴 지원에는 별도의 자격요건이 없습니다...

[2]
Q: 바이브코딩 인턴 선발절차를 알려주세요
A: 바이브코딩 인턴 선발절차는...

🔍 Checking Pinecone index...
✅ Index already exists

🔄 Generating embeddings and uploading...

[1/13] 바이브코딩 인턴 지원자격이 어떻게 되나요?...
  ✓ Uploaded
[2/13] 바이브코딩 인턴 선발절차를 알려주세요...
  ✓ Uploaded
[3/13] 바이브코딩 인턴 활동기간은 얼마나 되나요?...
  ✓ Uploaded
...
[13/13] ...
  ✓ Uploaded

✅ Upload completed!
📊 Total: 13 Q&A pairs uploaded
```

### 업로드 확인
Pinecone 콘솔에서 `qa-chatbot` 인덱스를 확인하면 13개의 벡터가 보여야 합니다.

## 2단계: 개발 서버 실행

```bash
npm run dev
```

## 3단계: 챗봇 테스트

브라우저에서 [http://localhost:3000](http://localhost:3000) 접속 후 다음 질문들을 시도해보세요:

### 정확히 일치하는 질문
```
바이브코딩 인턴 지원자격이 어떻게 되나요?
```
**기대 결과**: 신뢰도 95%+ 로 정확한 답변 반환

### 유사한 질문 (의미적 검색 테스트)
```
인턴에 지원하려면 무슨 자격이 필요한가요?
```
**기대 결과**: 신뢰도 75-90%로 유사한 답변 반환

### 완전히 다른 질문 (Hallucination 방지 테스트)
```
오늘 날씨가 어때요?
```
**기대 결과**: "정확한 답변을 제공할 수 없습니다" 메시지 (신뢰도 < 75%)

## 문제 해결

### 문제 1: `PINECONE_API_KEY is not defined`
**원인**: `.env.local` 파일이 없거나 잘못됨

**해결**:
```bash
# .env.local 파일 생성
PINECONE_API_KEY=your_actual_key
PINECONE_INDEX_NAME=qa-chatbot
GEMINI_API_KEY=your_actual_key
```

### 문제 2: `Error generating embedding`
**원인**: Gemini API Key가 유효하지 않음

**해결**:
1. [Google AI Studio](https://aistudio.google.com/app/apikey)에서 새 키 생성
2. `.env.local` 파일의 `GEMINI_API_KEY` 업데이트
3. 서버 재시작 (`Ctrl+C` → `npm run dev`)

### 문제 3: `Index not found`
**원인**: Pinecone 인덱스가 생성되지 않음

**해결**:
1. [Pinecone Console](https://app.pinecone.io/) 접속
2. "Create Index" 클릭
3. 다음 설정으로 생성:
   - Name: `qa-chatbot`
   - Dimensions: `768`
   - Metric: `cosine`
4. 1분 대기 후 `npm run upload` 재시도

### 문제 4: 업로드 중 `Timeout` 에러
**원인**: 네트워크 또는 Pinecone 서버 지연

**해결**:
- 스크립트가 자동으로 재시도하므로 잠시 기다리기
- 또는 `Ctrl+C`로 중단 후 `npm run upload` 재실행 (중복 업로드는 자동으로 덮어씀)

## 다음 단계

1. **Vercel 배포**: [README.md](README.md)의 "Vercel 배포 가이드" 참고
2. **커스터마이징**: 자신만의 Q&A 데이터로 교체
3. **UI 개선**: `app/page.tsx`에서 스타일 변경

## 핵심 파일 위치

- **API 로직**: `app/api/chat/route.ts` (유사도 임계값 조정)
- **UI**: `app/page.tsx` (디자인 수정)
- **업로드 스크립트**: `scripts/upload-data.ts` (Excel 파싱 로직)
- **데이터**: `data/` 폴더에 `.xlsx` 파일 추가
---

**소요 시간**: 약 5분 (업로드 2분 + 서버 시작 1분 + 테스트 2분)

궁금한 점이 있다면 [README.md](README.md)를 참고하세요!
